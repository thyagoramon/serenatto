{
  "name": "serenatto-cafe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass-embedded": "^1.87.0",
    "vite": "^6.3.5",
    "vite-plugin-gh-pages": "^1.0.1"
  }
}
